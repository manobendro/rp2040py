# RP2040 Emulator in python using unicorn engine (CPU Emulator based on qemu)

- Python based emulator to explore RP2040

## Editor setup

- Create a venv install requirement.txt
- Install python extension
- Select python interpretar as venv python using `Python: Select Interpreter` command
